# QA-исправления от 10 августа 2026 г.

1. Все доказательные условия в release-коде заменены явными
   `require(...)/raise`; режим `python -O` больше не отключает проверки.

2. `matrix_power` и обе независимые реализации отклоняют
   отрицательную и нецелую степень.

3. Усилены обязательные проверки порядка, размера, профиля
   степеней, триангуляции S^2, следов и точных знаков.

4. Добавлен строгий автономный graph6-декодер с проверками
   канонического заголовка, длины и нулевого padding.

5. Добавлены вторая полностью stdlib-реализация и офлайн
   property/regression-тесты.

6. Единый runner пишет только в `reproduced/`, не перезаписывает
   подписанные входы и сравнивает выводы обычного и -O режимов.

7. Исправлен путь запуска `independent_fullerene_audit_ru.py`.

8. Уточнено, что `check_package_consistency_ru.py` выполняет
   внутрипакетную сверку, а не независое доказательство.

9. Вывод смешанного шестого момента исправлен до
   `tr((A1+xD1)^6)-tr((A2+xD2)^6)=24(1-x^3)`.

10. Входные файлы внешнего аудита с жёсткими путями помечены
    `.original.txt` и не подмешиваются в release-запуск.
